{
  "name": "thegivehub",
  "version": "1.0.0",
  "main": "app.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "loadtest": "node scripts/load_test.js",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --headless",
    "cypress:record": "cypress run --record --headless --key $CYPRESS_RECORD_KEY",
    "cypress:run:local": "APP_ENV=testing CI=true TEST_ADMIN_TOKEN=TEST_ADMIN BASE_URL=http://localhost:8080 cypress run --headless",
    "cypress:open:local": "APP_ENV=testing CI=true TEST_ADMIN_TOKEN=TEST_ADMIN BASE_URL=http://localhost:8080 cypress open"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cypress": "^14.5.4",
    "firebase": "^11.0.2"
  },
  "devDependencies": {
    "autocannon": "^8.0.0",
    "cypress-mochawesome-reporter": "^4.0.0"
  }
}